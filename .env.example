# GEMINI_API_KEY: Required for all AI features (writing tools + AI Score Checker).
# Get yours free at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"

# ZEROGPT_API_KEY: Optional — only needed if you want ZeroGPT-based AI detection.
# The AI Score Checker works without this (uses Gemini instead).
# ZEROGPT_API_KEY="your-zerogpt-api-key-here"
